export enum LoginType {
  Kakao = 'kakao',
  Apple = 'apple',
}

export enum Gender {
  M = 'M',
  W = 'W',
}

export enum ErrorMessage {
  NEED_SIGN_UP = '회원가입이 필요해요!',
}

export enum ErrorCode {
  NEED_SIGN_UP = 700,
}
